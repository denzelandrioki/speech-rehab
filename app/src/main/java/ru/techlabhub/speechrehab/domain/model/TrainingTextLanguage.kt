package ru.techlabhub.speechrehab.domain.model

/** Как показывать подпись слова на карточке (независимо от языка UI). */
enum class TrainingTextLanguage {
    RUSSIAN,
    ENGLISH,
    BOTH,
    NONE,
}
